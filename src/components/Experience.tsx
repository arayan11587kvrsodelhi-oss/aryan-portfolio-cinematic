import { useEffect, useRef } from 'react'
import gsap from 'gsap'
import { ScrollTrigger } from 'gsap/ScrollTrigger'
gsap.registerPlugin(ScrollTrigger)
const ENTRIES=[['In Progress','BCA','Trinity Institute of Professional Studies','Programming, databases, web development and core computer science coursework.'],['Ongoing','Personal Development','Self-directed','Building and shipping projects to learn by doing — from UI experiments to full applications.'],['Ongoing','Cybersecurity','Self-directed learning','Studying web and network security fundamentals through guided learning and applied projects.']]
export default function Experience(){const ref=useRef<HTMLElement>(null);useEffect(()=>{const ctx=gsap.context(()=>gsap.fromTo('.timeline-row',{clipPath:'inset(0 100% 0 0)',opacity:0},{clipPath:'inset(0 0% 0 0)',opacity:1,stagger:.15,duration:.9,ease:'power4.out',scrollTrigger:{trigger:ref.current,start:'top 75%'}}),ref);return()=>ctx.revert()},[]);return <section ref={ref} className="px-6 md:px-10 py-[var(--spacing-section)] border-t border-border"><div className="max-w-container mx-auto"><span className="text-eyebrow">Education &amp; Growth</span><div className="mt-10">{ENTRIES.map(([period,title,place,desc])=><div key={title} className="timeline-row grid md:grid-cols-12 gap-4 py-8 border-b border-border"><span className="md:col-span-2 text-eyebrow text-accent">{period}</span><h3 className="md:col-span-4 font-display text-2xl">{title}</h3><span className="md:col-span-3 text-muted text-sm">{place}</span><p className="md:col-span-3 text-muted text-sm leading-relaxed">{desc}</p></div>)}</div></div></section>}
