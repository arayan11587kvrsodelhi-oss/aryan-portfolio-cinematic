import { useEffect, useRef } from 'react'
import gsap from 'gsap'
import { ScrollTrigger } from 'gsap/ScrollTrigger'
gsap.registerPlugin(ScrollTrigger)
const SERVICES=[['01','Web Development','Responsive, structured websites designed to look sharp and perform across devices.',['React','TypeScript','Tailwind']],['02','Frontend Development','Modern interfaces with polished interaction detail and reusable component systems.',['JavaScript','CSS','React']],['03','UI / UX Implementation','Turning visual ideas into responsive, accessible and production-ready interfaces.',['Figma → Code','Design Systems']],['04','Interactive Experiences','Scroll choreography, motion and micro-interactions used with intent.',['GSAP','ScrollTrigger','Framer Motion']]]
export default function Services(){const ref=useRef<HTMLElement>(null);useEffect(()=>{const ctx=gsap.context(()=>{gsap.fromTo('.service-row',{x:-45,opacity:0},{x:0,opacity:1,stagger:.12,duration:.8,ease:'power3.out',scrollTrigger:{trigger:ref.current,start:'top 72%'}})},ref);return()=>ctx.revert()},[]);return <section id="services" ref={ref} className="px-6 md:px-10 py-[var(--spacing-section)] border-t border-border"><div className="max-w-container mx-auto"><span className="text-eyebrow">What I Do</span><div className="mt-10 divide-y divide-border border-t border-border">{SERVICES.map(([n,title,desc,tech])=><div key={String(n)} className="service-row group grid md:grid-cols-12 gap-4 md:gap-8 items-center py-9 md:py-12 px-2 -mx-2 hover:bg-surface/70 transition-colors"><span className="md:col-span-1 font-mono text-muted group-hover:text-accent">{n}</span><h3 className="md:col-span-3 font-display text-2xl md:text-4xl group-hover:translate-x-2 transition-transform">{title}</h3><p className="md:col-span-5 text-muted text-sm leading-relaxed">{desc}</p><div className="md:col-span-3 flex flex-wrap gap-2 md:justify-end">{(tech as string[]).map(t=><span key={t} className="text-eyebrow border border-border px-2 py-1 group-hover:border-accent/50 group-hover:text-accent transition-colors">{t}</span>)}</div></div>)}</div></div></section>}
